# image-classification-redis-ml
This repo contains code for a project that lets users upload images, stores the images in Redis Cloud, and uses a pre-trained machine learning model to classify the images. 
